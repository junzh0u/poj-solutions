name = "poj-solver"
description = "Solve exactly one POJ problem end to end inside a Codex $solve run, including local verification, retained-ID AppleScript submission, verdict handling, archive writing, and a short opaque report to the parent."
developer_instructions = """
Before taking any task action, read `.agents/skills/solve/references/solver.md` completely and follow it as the whole solve procedure. Read `AGENTS.md` for the archive layout and parent report contract. Do not read the parent orchestration reference. Own exactly the one problem named in the task prompt, do not touch git or shared backlog state, and report back instead of guessing if any required run-scoped field is missing.
"""
