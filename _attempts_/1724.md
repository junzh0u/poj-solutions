# 1724 ROADS — not accepted

Two submissions, both Wrong Answer. Parked because the evidence points at the judge rather than the solution, not because the approach ran out of road.

## The problem

Cheapest-by-length route from city 1 to city N under a coin budget K. Directed roads, each with a length and a toll.

## What was tried

Budget-constrained shortest path over states of `(city, coins spent)` rather than city alone — a longer route that costs less can beat a shorter expensive one, so a single-state Dijkstra settles the wrong thing. Edge lengths are positive, so an optimal route never repeats a city and total length is bounded by `(N-1) * maxLen`; states are bucketed by exact distance (Dial's, no heap) and a `(city, cost)` state is expanded only when it strictly beats the cheapest cost already recorded for that city. The destination's first dequeue at cost `<= K` is the answer.

An earlier priority-queue version hit 1.3s on an adversarial case; the bucket queue brought that to ~0.12s.

Submissions:

1. Run 25194360, 1886B — Wrong Answer. Bounds hardcoded to the stated maxima.
2. Run 25194365, 2653B — Wrong Answer. Bounds sized from the actual input, and the solver wrapped in a loop over concatenated datasets, on the theory that the judge packs several cases per file.

## Why this looks like the judge

Verified independently, not taken from the agent's report:

- The most recent Accepted for 1724 is **2026-01-25**. Before that, the problem collected an Accepted every one to two months.
- Between 2026-04-18 and 2026-06-26, five distinct users (`8208240725`, `vjudge5`, `sfsdufe202418440224`, `vjudge1`, `czhappy`) submitted 18 times: 16 Wrong Answer, one Runtime Error, one Compile Error. No Accepted.

Two arguments that do **not** support the theory, and should not be repeated:

- Blank Memory/Time on the failing rows means nothing. POJ leaves both blank for every non-Accepted verdict — problem 1000, which is healthy, looks identical.
- vjudge relays whatever its user wrote. It is a proxy, not a validator, so a vjudge Wrong Answer is not a correct solution being rejected.

## What was verified locally

The sample; three historical cases from POJ's own discussion board for this problem (expected 14, 30, 56 — all matched); 5000+ randomized differential cases against two independent references (brute-force path enumeration, and a Bellman-Ford DP written from scratch) at sizes up to the stated maxima; budget 0, unreachable destination, parallel edges and self-loops.

## Where to pick up

The validated source is not in the repo, since it was never accepted. Re-deriving it is a couple of hours at most; the algorithm above is the whole of it.

Before spending another submission, re-check `http://poj.org/status?problem_id=1724&result=0` for an Accepted dated after 2026-01-25. If one appears, the judge is fixed and this is worth another run. If the record still shows nothing but Wrong Answer across many users, submitting again only adds to it.
