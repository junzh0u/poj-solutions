---
problem: 1485
park: judge
parked: 2026-08-07
solvers:
  - claude-opus-5 2026-08-07 5 Wrong Answer
  - claude-fable-5 2026-08-07 1 Wrong Answer
recheck: http://poj.org/status?problem_id=1485
---

# 1485 Fast Food

Five submissions, five Wrong Answers, cap reached.

## The problem and the approach

Place `k` depots among `n` sorted restaurants to minimise the total distance from each restaurant to its nearest depot, and print the assignment.

Interval DP: an optimal solution partitions the sorted restaurants into `k` contiguous groups with each depot at its group's lower median, so `dp[g][i] = min over p of dp[g-1][p] + cost(p+1, i)` with `cost` in O(1) from prefix sums, and parent pointers recover the witness. About 1.2M operations, 0.04s on fifty worst-case inputs.

## Why the objective is not in doubt

- Differential-tested against a brute force written from the statement — every C(n,k) depot set, each restaurant to the closest depot — over 1000+ random trials, all agreeing.
- **Output-identical to two independently written accepted solutions** over 4660 exhaustively enumerated small inputs (every n ≤ 7, every coordinate subset of 0..9, every k): the discuss-board solution in message 352368 (claims POJ Accepted, 47ms) and morris821028's UVa 662 solution, UVa 662 being the same problem.
- Matches a board user's posted expected output line for line (message 135736) on three 200-restaurant chains — 66 of 66 lines, totals 144490 / 64170 / 39563.
- Matches the statement's sample byte-for-byte, checked against the raw `<pre>` bytes.
- The witness was validated the way a special judge would: `k` depots, ranges tiling 1..n, each depot inside its own range, printed total equal to the witness's own cost, and every restaurant served by a (tied-)closest depot — no violations over 200 large random inputs.

## What the five submissions varied

| # | Run ID | Size | Change |
|---|---|---|---|
| 1 | 25195192 | 4086B | `long long` + `std::cout`; on equal-cost splits the tied restaurant goes to the depot on its **right** |
| 2 | 25195205 | 4962B | same, tied restaurant to the depot on its **left** (measured: 115 sent right in run 1, all 330 sent left here) |
| 3 | 25195209 | 4574B | plain `int` + `printf` throughout |
| 4 | 25195213 | 5320B | arrays widened to n ≤ 1000 / k ≤ 200, positions defensively sorted, k clamped to n |
| 5 | 25195214 | 5672B | always plural `serves restaurants i to i`, rather than the sample's singular |

One further click, around 18:00, was silently dropped inside POJ's ~10s window and produced no row. It was retried cleanly and is not one of the five.

Every degree of freedom the output has was exercised and none moved the verdict: tie-break in both directions, arithmetic width, I/O layer, array bounds, input-order assumptions, and singular versus plural wording.

The trailing blank line is not the bug. Each sample `<pre>` block loses exactly one trailing newline in POJ's rendering — the input block ends `0 0` with none and the output block ends `= 8\n` — which is only consistent with the stored answer ending `= 8\n\n`, i.e. a blank line after the last case, which is what is emitted.

## Judge history, recorded but not acted on

`status-via-curl 1485 --all-users` on 2026-08-07, 20 rows reachable, verified by the parent rather than taken from the solve agent:

- Last Accepted **2025-11-21** (Donlin, GCC). Before that 2025-10-16 (SYK2156) — both outside the current window.
- Since then **15 consecutive non-accepts**: 2025-12-19 Runtime Error, 2026-01-07 and 01-08 two Compile Errors, 2026-05-08 Wrong Answer, then on **2026-07-29 eight Wrong Answers inside 90 minutes from six unrelated accounts** (do3, vjudge2, vjudge4, vjudge5, block3, not3), then these five.
- **Zero Accepted anywhere in 2026.**
- This account took 51 accepts on other problems the same day, several within the same hour, so the session and the submit path were healthy throughout.

This is suggestive but deliberately **not** parked as `judge`. The judge criterion in `AGENTS.md` §5 rests on `System Error`, a verdict that says nothing whatever about the submitted program; `Wrong Answer` is a verdict users earn honestly, and the 2025 accepts show the data was sound then. Six accounts converging on Wrong Answer is also what a vjudge mirror looks like when many people submit the same widely-copied source.

## The control was run — judge park (2026-08-08, claude-fable-5 retry)

The experiment the paragraph below asked for was executed as the retry's first and only submission: the board message 352368 solution (KatrineYang, 2016-11-07, labelled POJ Accepted at 47ms), submitted **verbatim** as a diagnostic control. **Run 25195419, Wrong Answer, 1142B** — code length matches the extracted source byte count, so it arrived intact.

That verdict settles the split the control was designed for. A source this judge accepted in 2016, output-identical to the parked source over the 4660-case exhaustive domain, now draws WA. The parent re-verified the public record independently the same hour:

- Sole Accepted in the reachable 20-row window: Donlin, run 25053119, 2025-11-21, GCC, 0MS/728K — before the failure streak. **Zero Accepted anywhere in 2026.**
- After it, nothing but failures: the 2026-07-29 burst of **eight WAs in 90 minutes from six unrelated accounts** (do3 ×2, vjudge2 ×2, vjudge4, vjudge5, block3, not3 — G++ and GCC both represented, so this is not compiler folklore), plus WAs/CEs/RE from three more accounts, the park's five, and the control.
- Account health: 150014 took accepts on 2607, 2567, 3592, 1671, 2734, 2668, 1934 and more in runs 25195399–25195409, immediately adjacent to the control click — session and submit path healthy throughout.
- Every WA row carries empty time/memory fields where the 2025 accept shows 0MS/728K, consistent with the problem's special judge failing rather than programs computing wrong answers.

No further submissions were spent despite a raised cap of 10: the only source-side hypothesis left ("data silently grew past statement bounds") is already covered by the park's runs 1 and 4, and everything else would be blind permutation of exhausted axes.

**What retires this park:** any new Accepted from any user at the recheck URL. The solution itself is ready — the retry rebuilt it from the spec above and re-verified everything (sample byte-for-byte; all 4660 exhaustive cases with totals equal to the control's and every witness passing special-judge-style validation; 50 worst-case n=200/k=30 chains in 0.04s). A post-recovery retry is a rebuild-from-spec and one submission away.

## The original retry plan (executed above, kept for the record)

**Submit an independently-accepted source verbatim as a control, before anything else.** That is the experiment `AGENTS.md` §5 actually asks for, it was never run here, and it is the one thing that cleanly separates "this source is wrong" from "this judge is wrong". The board's message 352368 solution is the obvious candidate — but note it is output-identical to the parked source over 4660 exhaustive cases, so a Wrong Answer from it would be strong evidence about the judge rather than about either program.

If that control is Accepted, the fault is in this source's output on some case the 4660-case sweep and the 200-restaurant board cases do not reach, and the remaining unexplored surface is large inputs and the exact witness formatting. If the control is Wrong Answer, reclassify this park with `--kind judge --recheck http://poj.org/status?problem_id=1485` and the counts above.
